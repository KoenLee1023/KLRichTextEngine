# 架构、所有权与线程

软件包分为文档、序列化、编辑、排版和界面层。序列化层负责 `RichTextDocument`、JSON、RTF 与富文本字符串的转换。编辑层处理值类型文档并返回新值。界面层负责显示并把用户操作交给宿主。

文档和变更类型是值类型，在满足契约的地方实现 `Sendable`。存储、同步和数据库均由宿主持有。软件包不会自行启动同步任务，也不会访问数据库。

标记为 `@MainActor` 的界面 API 必须在主 actor 使用。纯编码器和文档转换不执行 I/O，可在合适的隔离上下文使用。校验会先于解析和依赖 UTF-16 区间的变更执行。
