# 아키텍처, 소유권, 스레드

패키지는 문서, 직렬화, 편집, 타이포그래피, UI 계층으로 나뉩니다. 문서와 편집 작업은 값 타입입니다. 저장, 동기화, 데이터베이스는 호스트가 소유하며 패키지가 자동으로 시작하지 않습니다.

`@MainActor` API는 메인 actor에서 호출해야 합니다. 순수 변환은 I/O를 수행하지 않습니다. 파싱하거나 UTF-16 범위를 사용하는 변경을 적용하기 전에 입력을 검증합니다.
