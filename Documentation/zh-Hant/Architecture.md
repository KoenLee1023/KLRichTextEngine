# 架構、所有權與執行緒

套件分為文件、序列化、編輯、排版及介面層。文件與變更類型是值類型，在契約允許時實作 `Sendable`。宿主擁有儲存、同步及資料庫。套件不會自行啟動同步工作，也不會存取資料庫。

標記為 `@MainActor` 的介面 API 必須在主 actor 使用。純編碼器和文件轉換不執行 I/O，可在適當的隔離環境使用。驗證會在解析及依賴 UTF-16 區間的變更之前執行。
