# アーキテクチャ、所有権、スレッド

パッケージはドキュメント、シリアライズ、編集、タイポグラフィ、UI の層に分かれています。ドキュメントと編集操作は値型です。保存、同期、データベースはホストが所有し、パッケージが自動的に開始することはありません。

`@MainActor` の API はメインアクターから呼び出してください。純粋な変換処理は I/O を行いません。解析や UTF-16 範囲を使う変更の前に入力を検証します。
